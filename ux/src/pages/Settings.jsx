import React from 'react';
import { Typography, Box } from '@mui/material';

const Settings = () => (
  <Box sx={{ p: 2 }}>
    <Typography variant="h5" sx={{ mb: 2 }}>
      Settings
    </Typography>
    <Typography>
      This is the Settings page. (Content to be implemented)
    </Typography>
  </Box>
);

export default Settings;
